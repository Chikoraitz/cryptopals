#include "../../include/set1/fixed_xor.h"

byte * xor(byte buffer1[], byte buffer2[]) {

  // Check if buffers are of equal length
  // Convert buffers to byte representation

  return "sadasdasdas";
}